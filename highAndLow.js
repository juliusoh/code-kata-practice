const highAndLow = (int) => {
  // const split = int.split(' ')
  // const arr = split.sort((a, b) => a - b)
  // const max = arr[arr.length - 1];
  // const min = arr[0]
  // return max + ' ' + min;

  
}


//sort method sorts the elements of an array in place and returns the sorted array. the default sort order is ascending. firstEl = element for comparison, secondEl second elemnt for comparison
console.log(highAndLow("1 2 -3 4 5"));